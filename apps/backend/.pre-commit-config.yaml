default_language_version:
  python: python3

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.22 # Must match required-version in pyproject.toml
    hooks:
      - id: ruff
        args: ["--fix"] # Fix lint & format

      - id: ruff-format # Format code
