Django==5.2.17
django-allauth==65.19.1
django-constance==4.3.5
django-cors-headers==4.9.0
django-cryptography-5==2.0.3
django-extensions==4.1
django-filter==25.2
django-bulk-update
django-silk==5.5.2
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.30.0
faiss-cpu==1.15.0
Flask==3.1.3
Flask-Cors==6.0.5
Flask-RESTful==0.3.10
geopy==2.5.0
uvicorn[standard]==0.53.0
hdbscan==0.8.44
nltk==3.10.3
markupsafe==3.0.3
Pillow==12.3.0
ImageHash==4.3.2
# insightface has no Windows wheel on PyPI; .github/workflows/windows-wheels.yml
# builds one (Python 3.11). Bump both lines together (Renovate is off for it).
insightface==0.7.3; sys_platform != "win32"
insightface @ https://github.com/LibrePhotos/librephotos/releases/download/windows-wheels-2026.09.16/insightface-0.7.3-cp311-cp311-win_amd64.whl ; sys_platform == "win32"
# stringzilla (insightface -> albumentations -> albucore): 5.1.2 has no Windows wheel.
stringzilla==5.1.1; sys_platform == "win32"
psycopg[binary]==3.3.4
https://github.com/owncloud/pyocclient/archive/master.zip
pytz==2026.3.post1
tzdata==2026.3
PyExifTool==0.4.9  # pinned: 0.5.x changes break metadata extraction; do not bump (see renovate.json)
# The images use the apt libvips (has JPEG XL and PDF loaders); the bundled
# one in pyvips-binary 8.18 does not, so it is Windows-only.
pyvips==3.1.1; sys_platform != "win32"
pyvips[binary]==3.1.1; sys_platform == "win32"
pyvips-binary==8.18.6; sys_platform == "win32"
# scikit-learn: the backend-gpu image runs Python 3.10 (CUDA/ubuntu22.04), but
# scikit-learn 1.8.0+ require Python >= 3.11. Keep the python_version < "3.11"
# pin at <= 1.7.2 or the GPU build fails. Renovate is disabled for this package
# (see renovate.json) so it can't re-collapse the split; bump it manually.
scikit-learn==1.9.0; python_version >= "3.11"
scikit-learn==1.7.2; python_version < "3.11"
# timezonefinder: same py3.10 split as scikit-learn. 8.2.1+ require Python >= 3.11,
# so the python_version < "3.11" pin must stay at 8.2.0 (last release supporting
# py3.10) or the GPU build fails. Renovate is disabled for it (see renovate.json).
timezonefinder==8.2.4; python_version >= "3.11" and sys_platform != "win32"
timezonefinder==8.2.0; python_version < "3.11"
timezonefinder @ https://github.com/LibrePhotos/librephotos/releases/download/windows-wheels-2026.09.16/timezonefinder-8.2.4-cp311-cp311-win_amd64.whl ; sys_platform == "win32"
tqdm==4.70.0
gevent==25.9.1
filetype==1.2.0
rawpy==0.27.1
django-q2==1.11.0
diskcache==5.6.3
safetensors==0.8.0
py-cpuinfo==9.0.0
psutil==7.2.2
concurrent-log-handler==0.9.29

# Tokenizers for the ONNX models: CLIP BPE (semantic search, MobileCLIP tags)
# and the LFM2 tokenizer (captions). The Rust package alone, no transformers.
tokenizers==0.22.2
argon2-cffi==25.1.0
# Dependencies for SigLIP 2 ONNX inference
# On the GPU image (Python 3.10) the Dockerfile swaps this for
# onnxruntime-gpu==<same version>. 1.26.0+ has no Python 3.10 wheel; 1.23.2 is the
# last release with a cp310 wheel for both onnxruntime and onnxruntime-gpu, and is
# the version the GPU image last built green with (before #1818). Keep the
# python_version < "3.11" pin at 1.23.2. Renovate is disabled for it (see renovate.json).
onnxruntime==1.27.0; python_version >= "3.11"
onnxruntime==1.23.2; python_version < "3.11"
# SentencePiece tokenizer for SigLIP 2
sentencepiece==0.2.2
# pyclipper: polygon offsetting for the PP-OCRv6 detector's DBPostProcess
# (unclip step). 1.4.0 ships cp310 AND cp311 wheels for manylinux2014_x86_64 and
# win_amd64, so both the GPU image (Python 3.10) and the standard image build.
pyclipper==1.4.0
