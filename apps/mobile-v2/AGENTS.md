# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before
writing any code. This project tracks **SDK 57** — the SDK the App Store build
of Expo Go for iOS runs. Do not move it ahead of (or leave it behind) that
client; see "Dependency constraints" in README.md.
