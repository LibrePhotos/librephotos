# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code. This project is deliberately held at **SDK 54** — the newest
SDK the App Store build of Expo Go for iOS can open. Do not "helpfully" upgrade
it; see "Dependency constraints" in README.md.
